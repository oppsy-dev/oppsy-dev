[project]
name = "test-package"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "copyparty (==1.20.0)",
    "multipart (==1.3.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
