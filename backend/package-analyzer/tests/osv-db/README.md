# OSV DB Test Data

Mocked OsvDb data folder containing pre-downloaded records used for testing. Records are downloaded using `scripts/fetch_osv_record.sh`.

## Adding a record

```bash
./scripts/fetch_osv_record.sh npm GHSA-hqjg-pww4-pcgq ./backend/package-analyzer/tests/osv-db/records
```
